# Copy to .env (gitignored). Never commit secrets.
# Widget sitekey is public (in site/nominate.html). Secret stays server-side only.

TURNSTILE_SECRET=
TURNSTILE_HOSTNAMES=ghosts.agenticop.io,35.224.146.25,localhost,127.0.0.1

# Bounded funeral search (npm run search:seed) — JSON search APIs only, site: queries.
# Auto order: Brave → SerpAPI Bing → Google CSE. Microsoft Bing Web Search API is retired.
# BRAVE_SEARCH_API_KEY=
# SERPAPI_API_KEY=
# SERPAPI_MONTHLY_BUDGET=25
# GM_SEARCH_PERIOD_MS=111600000
# GM_SEARCH_QUERIES_PER_RUN=1
# GOOGLE_CSE_ID=
# GOOGLE_API_KEY=

# Optional bind overrides
# GM_PORT=27474
# GM_BIND=0.0.0.0
